snapshots:
    - name: dim_hosts
      relation: ref('host_ephemeral_source')
      config:
        schema: gold
        database: AIRBNB_PRO
        unique_key: HOST_ID
        strategy: timestamp
        updated_at: HOST_CREATED_AT
        dbt_valid_to_current: "to_date('9999-12-31')"