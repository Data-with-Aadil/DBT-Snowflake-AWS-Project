snapshots:
  - name: dim_bookings
    relation: ref('booking_ephemeral_source')
    config:
        schema: gold
        database: AIRBNB_PRO
        unique_key: booking_id
        strategy: timestamp
        updated_at: created_at
        dbt_valid_to_current: "to_date('9999-12-31')"

