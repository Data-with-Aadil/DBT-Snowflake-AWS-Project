snapshots:
    - name: dim_listings
      relation: ref('listing_ephemeral_source') 
      config:
        schema: gold
        database: AIRBNB_PRO
        unique_key: LISTING_ID
        strategy: timestamp
        updated_at: LISTING_CREATED_AT
        dbt_valid_to_current: "to_date('9999-12-31')"